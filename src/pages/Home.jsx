import { HeadSection, Layout, MenuSection, FooterSection } from "../Exporter";

export const Home = () => {
    return (
        <Layout>
            <HeadSection/>
            <MenuSection/>
            <FooterSection/>
        </Layout>
    )
}
